# .NET build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user

# Runtime SQLite database (seed data lives in migrations, not the file)
*.db
*.db-shm
*.db-wal

# Node / Vite
node_modules/
dist/
.vite/
*.local

# Editor / OS
.vs/
.idea/
.DS_Store
*.swp

# Claude Code — personal, not shared (CLAUDE.md and .claude/settings.json ARE shared)
.claude/settings.local.json
CLAUDE.local.md

# SPA build output (generated by Zmg.Web build)
src/Zmg.Api/wwwroot/
